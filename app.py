# Main entry point for running the Streamlit app.
# It imports and runs the dashboard defined in the streamlit_app directory.

from streamlit_app.dashboard import run_dashboard

if __name__ == "__main__":
    run_dashboard()
